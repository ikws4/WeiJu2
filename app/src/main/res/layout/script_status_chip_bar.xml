<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/chip_bar"
    android:layout_width="match_parent"
    android:layout_height="15.5dp">

    <TextView
        android:id="@+id/chip_new_version"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginRight="4dp"
        android:background="@drawable/chip_shape_rose_background"
        android:backgroundTint="@color/transparent_30"
        android:backgroundTintMode="multiply"
        android:paddingHorizontal="4dp"
        android:text="@string/home_new_version"
        android:textColor="@color/highlight_med"
        android:textSize="10sp"
        android:visibility="gone"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/chip_package"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@drawable/chip_shape_iris_background"
        android:backgroundTint="@color/transparent_30"
        android:backgroundTintMode="multiply"
        android:paddingHorizontal="4dp"
        android:text="@string/home_package"
        android:textColor="@color/highlight_med"
        android:textSize="10sp"
        android:visibility="gone"
        tools:visibility="visible" />

</LinearLayout>

